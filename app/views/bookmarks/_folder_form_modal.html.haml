#folder-create-form.modal.hide.fade
  .modal-header
    = button_tag(type: "button", class: "close", data: { dismiss: "modal" },
      aria: { hidden: "true" }) do
      &times;
    %h3 Create new folder
  .modal-body
