#folder_delete_modal.modal.hide.fade
  .modal-header
    = button_tag(type: "button", class: "close", data: { dismiss: "modal" },
      aria: { hidden: "true" }) do
      &times;
    %h3 Delete folder
  .modal-body
