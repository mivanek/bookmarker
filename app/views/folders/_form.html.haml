= form_for folder, remote: true do |f|
  = render 'shared/error_messages', object: f.object

  = f.label :name, "Folder name"
  = f.text_field :name

  = f.submit "Create folder", class: "btn btn-large btn-primary pull-right",
    remote: true
