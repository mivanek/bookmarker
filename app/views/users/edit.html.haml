%h3.text-center Change user settings
.row
  .span6.offset3
    = form_for @user do |f|

      = render 'shared/error_messages', object: f.object
      = render 'form', f: f

      = f.submit "Save changes", class: "btn btn-large btn-primary"
